Instruction:
Open the terminal & follow the below instruction
	Download the repo - https://github.com/lalith93kumar/rspec_test.git
	Move to the directory - cd rspec_test
	Install homebrew from http://brew.sh/
	Install Rvm  - brew install Rvm
	Install Rvm  - rvm install ruby
	Install Bunbler - brew install bunbler3
	Bundle install

Run :
    rspec spec/api_test_spec.rb

TestData: 
	Append api request in both the files like file1.txt & file2.txt

I have verified this test with around 10 lakhs test data.